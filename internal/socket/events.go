package socket

// EventAuthentication defines the name of authentication socket event.
const EventAuthentication = "authentication"

const EventUnauthenticated = "unauthenticated"

// EventAuthenticated defines the name of the event emitted when authentication is successful
const EventAuthenticated = "authenticated"

// EventException defines the name of the event emitted when an exception is encountered.
const EventException = "exception"
